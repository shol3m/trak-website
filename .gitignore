node_modules

# env files with secrets — never commit
.env
.env.local
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Next.js build output
.next
out

# misc
.DS_Store

# MCP config with tokens — never commit
.mcp.json
.claude/settings.local.json

# Local Netlify folder
.netlify

# Playwright MCP logs and snapshots
.playwright-mcp/

# Dev screenshots (audit, search tests, font comparisons)
screenshots/

# Root-level woff2 font files (belong in public/fonts/)
*.woff2
