# ПРОГРЕСС ПРОЕКТА ТРАК

_Последнее обновление: 2026-08-07 (четвёртый проход)_

---

## Артикул выталкивал бренд за край карточки, WhatsApp-кнопка убрана (2026-08-07, четвёртый проход)

- **`components/ui/ProductCard.tsx`** — на узких экранах длинный артикул (например `S2512V21WBA15sORANGE`) выталкивал бренд товара за правый край карточки. Причина: артикул и бренд в одной `flex justify-between` строке, у артикула не было `min-w-0`/`truncate`, поэтому flex-элемент не сжимался ниже собственной ширины контента (дефолт `min-width: auto` для flex-детей). Добавлены `truncate min-w-0` артикулу — теперь обрезается многоточием, бренд (`shrink-0`) остаётся на месте. `ProductListRow` (список-режим в `CatalogView.tsx`) бренд не выводит вообще — там бага не было.
- **`components/ui/WhatsAppButton.tsx`** — убран из рендера в `app/layout.tsx` по просьбе заказчика. Файл не удалён.

---

## Мобильная адаптация: hero, категории каталога, отступ под хедером (2026-08-07, третий проход)

Серия точечных мобильных фиксов по скриншотам заказчика:

1. **`HeroSlider.tsx`** — на мобиле заголовок (`text-2xl`) терялся на фоне большого фикс-баннера (`420px` высотой на всех экранах). Текст увеличен на шаг по всей шкале (`text-2xl→5xl` → `text-3xl→6xl`), высота баннера сделана отзывчивой (`300px` на мобиле, `420px` от `sm:`, было флэт `420px` везде). Заодно исправлена устаревшая заметка в `CLAUDE.md` — там было написано `text-4xl→7xl`, реальный код к этому моменту уже был сжат до `2xl→5xl` в одной из прошлых сессий без обновления доки.
2. **`app/catalog/CategoryTiles.tsx`** — плитки подкатегорий на `/catalog` съедали весь экран на мобиле (15 корневых категорий, `grid-cols-2`, часть названий переносилась на 2 строки). Через `/impeccable` (`посмотри каталог на мобилке, как правильно сделать категории`) — решение: на мобиле не сетка, а горизонтальная прокрутка в одну строку (`overflow-x-auto snap-x snap-mandatory`, чистый CSS без новых зависимостей, скроллбар спрятан `.no-scrollbar` в `globals.css`), текст не обрезается — полное название видно прокруткой. От `sm:` — прежняя сетка. Компонент переиспользуется и на `/catalog/[...path]` для подкатегорий (там категорий обычно меньше, паттерн не мешает).
3. **Отступ под хедером `pt-24` → `pt-8`** — заказчик спросил, почему большая дыра между хедером и контентом на `/catalog` **на всех устройствах** (не мобильный баг, значение без брейкпоинтов). Причина: `pt-16` в `app/layout.tsx` уже точно закрывает высоту хедера (64px), а `CatalogView.tsx`/`CartView.tsx`/`catalog/[...path]/page.tsx`/`product/[article]/page.tsx` поверх этого добавляли ещё `pt-24` (96px) — остаток с эпохи, когда над хедером ещё рендерился `TopBar` (убран 2026-08-03), значение не пересчитали. Исправлено единообразно во всех 4 местах на `pt-8`.

---

## Промпты для фото акций (2026-08-07)

Заказчик отметил, что плашки акций в `PromoCampaigns.tsx` — просто иконка на сером фоне, хотел настоящие «акционные» фото. Код уже готов: `lib/mock-data.ts` → `CAMPAIGN_IMAGES` (пустой `Record<string, string>`, ключ — `id` кампании), `PromoCampaigns.tsx` рендерит фото вместо иконки, когда есть запись в `CAMPAIGN_IMAGES`. Стиль — тот же тёмный студийный фон, что у фото категорий, но с **красным** акцентным светом (не синим) — фирменный цвет действия, отличает «акционные» карточки от товарных фото категорий. Общий постфикс для промпта: *«professional studio product photography, dark charcoal-to-black gradient background, dramatic single-source red-tinted accent lighting (matches brand red #C8102E), items arranged at a slight diagonal angle, sharp focus, high detail, square 1:1 composition, no text, no logos, no watermarks, no hands, no people»*.

1. **`campaign-1.webp`** (id `1`, «В наличии — без ожидания») — an oil filter, a boxed headlight bulb, and a brake pad set, arranged as if just pulled off a shelf, slight sense of motion.
2. **`campaign-2.webp`** (id `2`, «Расходники до 500 ₽») — a handful of bolts, a small fuse, and a compact oil filter, with one blank paper price tag hanging off a string (no text/numbers on the tag).
3. **`campaign-3.webp`** (id `3`, «Оригинал ГАЗ и УАЗ») — a headlight assembly with a blank holographic authenticity foil seal on it (no text/brand marks on the seal).
4. **`campaign-4.webp`** (id `4`, «Замена масла бесплатно») — an oil canister, an oil filter, and a funnel, with a single oil droplet frozen mid-drip.

Формат — WebP, ~1200-1500px, квадрат, как у категорий. Класть в `public/images/campaigns/campaign-<id>.webp`, добавить строку в `CAMPAIGN_IMAGES` (`lib/mock-data.ts`) — тем же способом, что и категории: прислать файлом в чат или положить прямо в папку.

---

## Акции → вертикальные баннеры, промпты фото категорий, PRODUCT.md (2026-08-06, второй проход)

Продолжение вчерашнего разбора блока «Акции» (см. запись ниже, «Категории на проде...») — заказчик подтвердил направление живьём в сессии. Прошли через `/impeccable init` (создан `PRODUCT.md` в корне — аудитория розница/опт поровну, позиционирование офиц. дилера ГАЗ/Соллерс, ключевой факт: новые изображения — в основном GPT-генерация, реальных фото объекта пока нет) → `/impeccable shape` (согласован бриф) → реализация:

- **`app/page.tsx`** — `PromoCampaigns` переставлен под `HeroSlider` (было первым блоком под Header).
- **`components/sections/PromoCampaigns.tsx`** — переписан с кружков-«сторис» (градиентный красно-синий ободок, клик → `StoryViewer`) на 4 вертикальные карточки-баннера (`grid-cols-2 lg:grid-cols-4`, `aspect-[3/4]`): фото-слот сверху (~65% высоты, сейчас — иконка-плейсхолдер на `bg-muted`, тот же fallback-паттерн что у `CategoryGrid`) + заголовок/описание/CTA-стрелка снизу, вся карточка — `Link` сразу на `href` из `mockCampaigns` (без промежуточного клика в модалку). `StoryViewer.tsx` больше нигде не используется — файл не удалён.
- **`lib/category-icons.tsx`** — оставлен как есть технически (`CATEGORY_IMAGES`), добавлен только комментарий-очередь на 7 новых фото категорий (список ниже). Добавлять запись в `CATEGORY_IMAGES` только когда файл реально существует в `public/images/categories/` — не раньше, чтобы не сломать рендер битой картинкой.

### Промпты для ChatGPT (7 категорий, тот же стиль что `avtosvet`)

Референс — уже готовое `public/images/categories/category-avtosvet.webp`: тёмный студийный градиентный фон (почти чёрный → тёмно-серый), драматичный боковой свет, 2-3 детали в композиции под небольшим углом, фотореалистичная предметная съёмка, квадрат 1:1, без текста/логотипов/водяных знаков, без рук и людей. Заказчик сам прогоняет промпты через ChatGPT и загружает результат.

Общий постфикс для каждого промпта: *«professional studio product photography, dark charcoal-to-black gradient background, dramatic single-source side lighting, items arranged at a slight diagonal angle, sharp focus, high detail, square 1:1 composition, no text, no logos, no watermarks, no hands, no people»*.

1. **`category-avtozapchasti.webp`** (Автозапчасти) — a brake disc, a set of brake pads, and a CV joint / drive shaft arranged together.
2. **`category-masla.webp`** (Масла) — a motor oil canister/bottle, an oil filter, and a funnel.
3. **`category-akkumulyatory-i-zaryadka.webp`** (Аккумуляторы и зарядка) — a car battery with a battery charger and jumper cable clamps.
4. **`category-avtohimiya-i-avtokosmetika.webp`** (Автохимия и автокосметика) — a chemical spray bottle, a polish bottle, and a microfiber cloth.
5. **`category-avtoelektronika.webp`** (Автоэлектроника) — a dash camera, a relay/fuse box, and a wiring connector.
6. **`category-instrumenty.webp`** (Инструменты) — a socket wrench set, a ratchet, and a few sockets arranged together.
7. **`category-schetki-stekloochistitelya.webp`** (Щётки стеклоочистителя) — two windshield wiper blades crossed at an angle with a washer fluid bottle.

Формат вывода — как у `avtosvet`: WebP, ~1200-1500px, квадрат. Пути и имена файлов уже зафиксированы в списке выше — класть в `public/images/categories/`, после чего добавить соответствующую строку в `CATEGORY_IMAGES` (`lib/category-icons.tsx`).

**Осознанно поменяно по ходу сессии**: изначально в топ-8 предлагались «шины/диски» — заказчик попросил заменить на что-то другое, выбраны «щётки стеклоочистителя» (частая повторная покупка, легко узнаваемый предмет).

**Первое фото уже получено и подключено** (2026-08-06, тем же днём) — заказчик сгенерировал фото для `avtoaksessuary` (Автоаксессуары, категория вне топ-7 выше — регистратор, держатель телефона, автомобильное зарядное устройство) и прислал файлом в чат (не ссылкой на ChatGPT — попытка открыть share-ссылку через браузер была отклонена, заказчик предпочитает просто прикладывать файл). Тот же формат: WebP 1254×1254, тёмный градиентный фон, боковой свет. Сохранён в `public/images/categories/category-avtoaksessuary.webp`, добавлена строка в `CATEGORY_IMAGES` (`lib/category-icons.tsx`).

**2026-08-07 — ещё 4 фото подключено** (`avtozapchasti` файлом в чат; `masla`, `akkumulyatory-i-zaryadka`, `avtohimiya-i-avtokosmetika` — заказчик положил файлы напрямую в `public/images/categories/` под именами `masla.webp`/`akkumulyatory.webp`/`avtohimiya-i-avtokosmetika.webp`, переименованы в конвенцию `category-<slug>.webp` и подключены). Итого готово 6 из 8: `avtosvet`, `avtoaksessuary`, `avtozapchasti`, `masla`, `akkumulyatory-i-zaryadka`, `avtohimiya-i-avtokosmetika`. Осталось 3: `avtoelektronika`, `instrumenty`, `schetki-stekloochistitelya` (промпты — см. выше).

**2026-08-07 — `CategoryGrid` на главной сужена до одного ряда**: заказчик попросил показать только 5 категорий с готовым фото (`avtoaksessuary`, `avtozapchasti`, `akkumulyatory-i-zaryadka`, `avtohimiya-i-avtokosmetika`, `masla`), явно скрыв `avtosvet` и все остальные из 15 — вместо большой сетки на главной теперь один плотный ряд. Реализовано через константу `FEATURED_SLUGS` в `CategoryGrid.tsx` (фильтр + фиксированный порядок поверх дерева категорий), секция сжата `py-20` → `py-12 sm:py-16`. Полное дерево категорий не изменилось, остальные категории по-прежнему доступны через `/catalog` — просто не выводятся на главной.

---

## Категории на проде, промо-блоки главной, Impeccable, аудит (2026-08-05/06)

Сессия началась с бага «на проде на Vercel нет категорий» и переросла в несколько независимых потоков работы. По порядку:

**1. Баг: категории пропадали на проде (не локально)** — два независимых бага в `getCategoryTree()` (`lib/db-catalog.ts`), оба тихо проглатывались `try/catch → return []`, поэтому в логах Vercel ничего не было видно:
- Запрос «категории с активными товарами» шёл через PostREST embedded-join (`Category → Product!inner`), RLS-политика `isActive = true` не давала планировщику использовать индекс для join-запроса → `57014 canceling statement due to statement timeout` на 280k товаров (тот же класс бага, что раньше чинили для поиска). Фикс: новая `SECURITY DEFINER`-функция `get_active_category_ids()` — `scripts/add-category-active-ids-function.mjs`.
- Даже после фикса №1 главная (статическая генерация при билде) оставалась пустой: Supabase-клиент всегда ставил `cache: 'no-store'`, что конфликтует с `unstable_cache()` при статической генерации (`Error: Dynamic server usage`). Фикс: отдельный клиент `supabaseCached` в `lib/supabase.ts` (без `no-store`), используется только внутри `unstable_cache`.
- Оба фикса задеплоены на прод, проверено вживую (bot-checkpoint Vercel сначала мешал curl/Playwright — обошли через реальный браузер).

**2. Главная: новые промо-блоки** (после нескольких итераций и одной путаницы в терминологии — см. ниже):
- `components/sections/CategoryGrid.tsx` (новый) + `lib/category-icons.tsx` (новый) — сетка из 15 иконок по корневым категориям, поддержка реальных фото через `CATEGORY_IMAGES` (пока подключено одно — `avtosvet`, `public/images/categories/category-avtosvet.webp`, WebP 1254×1254 сгенерирован заказчиком через ChatGPT по предоставленному промпту). Остальные 14 категорий ждут фото.
- `components/sections/PromoCampaigns.tsx` (новый) + `components/ui/StoryViewer.tsx` (новый) — блок «Акции», 4 кружка-«сторис» (промо-кампании из `lib/mock-data.ts` → `mockCampaigns`), клик открывает полноэкранный просмотрщик в стиле Instagram Stories (автоплей 5с/слайд, прогресс-бар, тап-зоны, свайп между акциями).
- **Путаница и фикс**: заказчик просил «Акции наверх вместо плитки с категориями каталога» — я по ошибке убрал `CategoryGrid` (новую секцию, которую сам же в этот момент добавлял), хотя имелась в виду `CategoryNavTabs` (вкладки-«пилюли» под хедером). Исправлено: `CategoryNavTabs` убран из `app/page.tsx` (файл не удалён), `CategoryGrid` возвращён на прежнее место, `PromoCampaigns` — первым блоком под хедером.
- **После живого просмотра заказчик отметил, что блок акций «не то»** — самостоятельный разбор (не запрошен инструментом): круглые градиентные (красный→синий) «пузыри» — единственное на сайте место, где смешаны оба фирменных цвета и единственный скруглённый элемент (весь сайт — плоский, прямые углы, один акцент на действие). Отсутствие заголовка усиливает эффект «непонятного виджета» перед hero, который должен первым объяснять, кто мы. **План на следующую сессию** — см. ЧТО НЕ РЕАЛИЗОВАНО в `CLAUDE.md`.
- Заодно: «50 000+» → «10 000+» везде, где реально рендерится (`HeroSlider`, `StatsBrandsRow`, `app/about/page.tsx` ×4, `CategoryGrid` подзаголовок) — неиспользуемые черновики (`HeroBanner.tsx`, `HeroBannerIcon.tsx`, `CategoriesSection.tsx`) не трогали.
- Промежуточная версия «Акции» была компактнее (`py-20` секция с заголовком) — по запросу заказчика сжата до узкой полосы `py-4` без заголовка, кружки 80px→56px (эта версия и есть предмет разбора выше).

**3. Impeccable + пайплайн-скиллы установлены в проект** (`.claude/skills/`) — не глобально:
- `impeccable` (`pbakaus/impeccable`, реальный проверенный автор/репо) — детектор «AI slop»-паттернов + 20+ дизайн-команд (`critique`/`audit`/`polish`/`typeset`/...). Установка добавила **hooks** в `.claude/settings.local.json` — автоматическая проверка после каждого `Edit`/`Write` + полный проход при завершении сессии.
- Заодно по явной просьбе поставлены `autopilot` + пайплайн `mattpocock/skills` (`setup-matt-pocock-skills`, `grilling`, `to-spec`, `to-tickets`, `implement`) — оркестратор «идея → спека → тикеты → реализация» для крупных фич под ключ.
- **Важно для будущих сессий**: в исходниках `impeccable` (`scripts/context.mjs`) обнаружены два блока (`AUTONOMY_DIRECTIVE_CHECK`, `SUBAGENT_AUTHORIZATION`), которые пытаются через вывод инструмента убедить модель игнорировать системные инструкции про автономность и трактовать вызов скилла как разрешение на саб-агентов без подтверждения — классический prompt injection через сторонний пакет. Решение: эти два блока **игнорируются целиком**, остальной функционал (генерация `DESIGN.md`, аудит, детектор) — используется, он оказался настоящим и полезным. Каждая установка (`npx skills add ...`, `npx impeccable install`) была проверена по исходникам на GitHub/npm перед запуском (легитимный maintainer, отсутствие `postinstall`-хуков, отсутствие `exec`/`eval` в коде), а не установлена вслепую.
- `docs/DESIGN.md` полностью перегенерирован (`/impeccable document`) из реального кода (Tailwind-конфиг, `globals.css`, компоненты), а не оставлен как есть — старая версия документировала шрифты и токены, отменённые ещё 2026-08-04 (Roboto Condensed/IBM Plex Sans/IBM Plex Mono вместо реальных Oswald/Inter/JetBrains Mono). Новый файл честно фиксирует и находки вроде мёртвого токена `brand.gold` (`#D4A017` в `tailwind.config.ts`, нигде не используется — реальный золотой всюду `#C4922A` из `globals.css`).
- `/impeccable audit` на главной нашёл 2 реальных находки, обе исправлены: (P1) `HeroSlider.tsx` рендерил `<h1>` на каждый из 3 слайдов одновременно (Swiper с `effect="fade"` держит все слайды в DOM) — теперь `<h1>` только у активного слайда (`onSlideChange` + `activeIndex`), у остальных — визуально идентичный `<p>`; (P2) тач-таргеты степперов количества 28–40px вместо минимальных 44px — исправлено во всех 5 местах, где встретился этот паттерн (`ProductCard`, `CatalogView`, `CartView`, `CartDrawer` — там же заодно добавлены отсутствовавшие `aria-label`, `AddToCartButton`).

---

## Страница /cart вместо только drawer (2026-08-04, восьмой проход)

Последний пункт из плана заказчика «Приоритет 0.5». Обсуждали два варианта: (1) урезать `CartDrawer` до превью-режима, чек-аут только на `/cart`; (2) вынести форму чек-аута в переиспользуемый компонент, использовать и в drawer, и на странице, не трогая поведение drawer. Заказчик подтвердил второй вариант после явного вопроса «нужно ли трогать drawer» — он не хотел лишнего риска для рабочего потока ради задачи, которая формально требовала только «добавить страницу».
- **`components/ui/CheckoutForm.tsx`** (новый) — вся форма чек-аута (имя/телефон/комментарий, honeypot, rate-limit 30с, POST `/api/order`, экран успеха) вынесена из `CartDrawer.tsx` один в один, без изменения логики. Пропы: `onSuccessContinue?` (что делает кнопка «Продолжить покупки» после успеха — drawer передаёт `closeCart`, страница не передаёт и получает `Link` на `/catalog`), `onOrderPlaced?` (коллбэк в момент успеха, до `clearCart()`).
- **`components/ui/CartDrawer.tsx`** — убраны все стейты/хендлеры формы (name/phone/comment/honeypot/status/rateLimit/lastSubmitRef/validateName/validatePhone/handleSubmit/inputCls), вместо инлайн-формы — `<CheckoutForm onSuccessContinue={closeCart} />`. Внешнее поведение (переключение `view` cart↔checkout, кнопка «Назад», сброс `view` при закрытии) не изменилось — проверено вручную через Playwright (добавить товар → «Оформить заказ» в drawer → форма на месте, как раньше).
- **`app/cart/page.tsx`** + **`app/cart/CartView.tsx`** (новые) — полноценная страница: список товаров со степпером слева, `CheckoutForm` в sticky-карточке справа (`lg:grid-cols-[1fr_380px]`, на мобиле — в колонку).
- **Баг, пойманный и исправленный в этой же сессии**: при первой версии `CartView` список товаров и блок оформления рендерились в двух разных ветках условия (`items.length === 0 ? empty : items.length > 0 ? grid : ...`). После успешной отправки `clearCart()` обнуляет `items` → компонент переключался на ветку «корзина пуста» → `CheckoutForm` **размонтировался** вместе со своим стейтом `status === 'success'` → пользователь видел пустую корзину вместо «Заказ принят!». Поймано визуально через Playwright (сразу после сабмита увидел пустое состояние вместо галочки). Фикс: `CheckoutForm` всегда рендерится в одном и том же месте дерева (сетка с левой/правой колонкой не пересоздаётся), только список товаров внутри левой колонки скрывается через `items.length > 0 && …`; локальный `orderPlaced` в `CartView` (поднимается через `onOrderPlaced`) отличает «корзина изначально пуста» от «корзина пуста, потому что заказ только что оформлен» для первого захода на страницу.
- Проверено Playwright MCP: отправка заказа с `/cart` (экран успеха остаётся на месте, кнопка «Продолжить покупки» ведёт на `/catalog`), обычная пустая корзина при заходе без заказа, чек-аут внутри `CartDrawer` (регрессия не найдена), мобильная раскладка 375px (колонка вместо сетки).
- **Найдено сразу после первой демонстрации**: на `/cart` в итоге невозможно было попасть ни из одного элемента UI — кнопка корзины в шапке открывает только drawer, «Оформить заказ» в drawer вёл на внутренний шаг чек-аута, не на страницу. Добавлена ссылка «Перейти в корзину» в `CartDrawer.tsx` (режим просмотра, рядом с «Очистить корзину») — закрывает drawer и ведёт на `/cart`.

---

## SectionHeading: капс вместо провисающей «У» (2026-08-04, внеочередной фикс)

Заказчик прислал скриншот заголовка «Услуги автосервиса» — буква «У» визуально проваливалась ниже строки, выглядело как рендер-баг.
- **Диагностика**: не баг рендера — реальная особенность контура буквы в файле шрифта. Через `fontTools` сравнил bounding box глифов Oswald (cyrillic subset): у У/Ц/Щ/Д `yMin = -147` (хвостик ниже базовой линии — стандартный приём кириллической типографики, у профессиональных шрифтов так же), у остальных прописных (например, Т) `yMin = 0`. На смешанном регистре («Услуги автосервиса») это читалось как «одна буква повисла», в капсе — незаметно, потому что все буквы визуально уравновешены отсутствием строчных.
- **Проверка гипотезы**: временно накинул `text-transform: uppercase` через `page.evaluate` в Playwright (без правки кода) — подтвердил на скриншоте, что капс убирает эффект.
- **Фикс**: `components/ui/SectionHeading.tsx` — добавлен класс `uppercase` на `<h2>`. Заказчик уточнил проверить, что это не повредит SEO — `text-transform` chисто визуальный CSS, не трогает текст в DOM (в отличие от `.toUpperCase()` в JS), поисковики и скринридеры видят оригинальный регистр из `title` пропа.
- Затронуло все страницы с `SectionHeading` (`/service`, `/about`, `ReviewsSection`) — проверено Playwright MCP на `/service` и `/about`, заголовки ровные, регресса не найдено.
- Другие `font-heading`-места без капса (цены, названия услуг/товаров из БД) не трогал — не входило в запрос, отдельное решение, если понадобится.

---

## Кикер-лейблы убраны + страница товара: buy-box (2026-08-04, шестой/седьмой проход)

Заказчик обсудил план из «Приоритет 0.5» вживую: кикеры, страница товара, каталог на мобилке.

**Кикеры.** Прошли несколько итераций макета через Playwright, прежде чем нашли решение:
1. Первая попытка (иконка рядом с заголовком в строку, вместо текста) — заказчику не понравилась сама идея.
2. Открыл живой rossko.ru через Playwright как референс — оказалось, там кикеров-текста нет вообще, только толстая цветная плашка-маркер над блоком, отдельно от заголовка (не вплотную, не подпись). Показал заказчику скриншот с их «Знаем всё о запчастях» / «Доставим по всей России».
3. Заказчик решил ещё жёстче — убрать вообще всё, оставить только сам заголовок, без плашки/иконки/текста.
- Итог: `SectionHeading.tsx` — проп `eyebrow` и рендер акцентной полоски (`w-8 h-0.5 bg-red`) удалены целиком (ни один вызывающий компонент `eyebrow` не передавал — TS не жаловался). То же самое вручную убрано в 8 местах, где кикер жил не через `SectionHeading`: `PartFinderCTA`, `HeroSlider` (бейдж-бордер на фото слайда + акцентная полоска под ним — поле `badge` в данных слайдов осталось, используется только как `alt` картинки), `ServiceSection`, `app/about/page.tsx` (hero + CTA-блок с телефоном), `app/contacts/page.tsx` (hero), `BookingModal`, `PartRequestModal`, `ServiceBookingCTA`. Функциональные микро-подписи (Email/WhatsApp/Адрес на `/contacts`, `<label>` полей в неиспользуемом `PartFinderSection`, заголовки колонок `Footer`) не трогали — это не декоративный кикер.
- Проверено Playwright MCP на `/`, `/about`, `/contacts`, `/service` — везде чисто, заголовки без плашек. По пути поймал ложную тревогу: `ServiceSection` на полностраничном скриншоте выглядел пустым (`opacity:0`) — не баг, а `framer-motion` `whileInView`, которая не триггерится при программном full-page screenshot без реального скролла; при реальном скролле (`scrollTo`) рендерится нормально.

**Страница товара** — направление «buy-box» (заказчик подтвердил сразу, без правок макета):
- `app/product/[article]/page.tsx` — цена и `AddToCartButton` объединены в блок `bg-bg-muted border p-5` вместо голой цены и кнопки россыпью. Подписи «Описание»/«Гарантия» — обычный `font-bold` текст вместо `uppercase tracking-wider` (тот же кикер-паттерн, что чистили выше).
- `app/product/[article]/AddToCartButton.tsx` — кнопка `size="lg"` → `size="md"` (заказчик отметил как слишком крупную). Заодно переведена на тот же паттерн, что `ProductCard`/`ProductListRow` в каталоге (сессия чуть раньше): товара нет в корзине → кнопка «Добавить в корзину»/«Заказать»; товар уже в корзине → инлайн-степпер (−/количество/+) вместо разового текста «✓ Добавлено» с `setTimeout`.
- Проверено Playwright MCP на живых товарах: с пустой корзиной (кнопка компактная) и с товаром уже в корзине (степпер внутри buy-box).

**Каталог на мобилке** — реализовано:
- `app/catalog/CatalogView.tsx` — сетка карточек: `grid-cols-1 sm:grid-cols-2 ...` → `grid-cols-2 lg:grid-cols-3 xl:grid-cols-4` (2 колонки от самого маленького экрана вместо карточки на всю ширину), `gap-4` → `gap-3 sm:gap-4`.
- Toolbar (поиск/сортировка/переключатель вида): был один `flex` ряд без переноса, на 375px поиск сжимался и плейсхолдер обрезался. Стал `flex-col sm:flex-row` — на мобиле поиск на всю ширину отдельной строкой, сортировка+переключатель вида второй строкой (`flex-1 sm:flex-none` на `<select>`, чтобы делил ширину с переключателем, а не сжимал его).
- Проверено Playwright MCP на 375px: карточки компактные 2-в-ряд, степпер количества помещается без переносов, поиск читается полностью. На 1440px раскладка не изменилась (4 колонки, sm:flex-row вернулся).

---

## Каталог: инлайн-степпер количества (2026-08-04, пятый проход)

Третий пункт из плана заказчика (`CLAUDE.md` → «Приоритет 0.5»): после клика «В корзину» карточка должна показывать счётчик количества вместо повторного показа кнопки.
- `ProductCard.tsx` — проп `onAddToCart` убран, компонент сам читает `useCartStore` (`items.find(i => i.product.id === product.id)?.quantity ?? 0`). `quantity === 0` → кнопка «В корзину»/«Заказать» (как раньше, `addItem` + `openCart`); `quantity > 0` → инлайн-степпер (−/число/+, `updateQuantity`), клики по степперу **не** открывают `CartDrawer` — иначе набор нескольких позиций подряд постоянно перебивался бы открытием панели.
- `CatalogView.tsx` — тот же паттерн продублирован в локальном `ProductListRow` (list-режим таблицы). `onAddToCart`-проп и `addItem`/`openCart` в самом `CatalogView` убраны — были нужны только для передачи в дочерние карточки.
- `ProductsSection.tsx` (неиспользуемый компонент главной, см. `CLAUDE.md`) — обновлён под новый API `ProductCard` (убраны `onAddToCart`/`useCartStore`), чтобы не сломать типы, раз он использует тот же компонент.
- Проверено Playwright MCP: сетка и список, клик «В корзину» → степпер появляется на месте кнопки, «+» увеличивает счётчик в шапке (2) без открытия корзины, значение синхронно между сеткой/списком (общий `useCartStore`).

---

## Корзина: emoji → SVG + текст поля комментария (2026-08-04, четвёртый проход)

Второй пункт из плана заказчика (`CLAUDE.md` → «Приоритет 0.5»), оба — правки `CartDrawer.tsx`.
- 🛒 (пустая корзина) → SVG-иконка корзины (та же линия/толщина, что остальные SVG в файле, `text-text-ghost`)
- ✅ (экран «Заказ принят») → SVG-кружок с галочкой, `#16A34A` (зелёный, единственное новое цветовое значение в файле — остальной UI использует акценты из семантических токенов, но статус «успех» ни один токен не покрывает)
- Плейсхолдер поля «Комментарий»: «Адрес доставки или дополнительная информация» → «Дополнительная информация по заказу» — заказчик уточнил, что это тоже нужно поправить (раньше сознательно не трогали, посчитав рабочим полем формы, а не маркетинговым текстом, см. P11)
- Проверено визуально Playwright MCP (Claude in Chrome в этой сессии не подключился): пустая корзина, чекаут-форма и экран «Заказ принят!» после реальной отправки через `/api/order` — обе SVG-иконки и новый текст плейсхолдера рендерятся корректно

---

## Футер: реальные категории вместо хардкода (2026-08-04, третий проход)

Первый готовый пункт из плана заказчика (`CLAUDE.md` → «Приоритет 0.5»). Колонка «Каталог» в `Footer.tsx` держала хардкод 5 старых flat-категорий («Двигатели», «Фильтры»...), все ссылки вели просто на `/catalog` — это slug'и, которых с 2026-08-01 нет в реальном дереве (см. «Slug ↔ категория» в `CLAUDE.md`).
- `app/layout.tsx` — `RootLayout` стал `async`, вызывает `getCategoryTree()` (тот же паттерн, что `app/page.tsx` → `CategoryNavTabs`) и передаёт первые 5 корневых категорий (`.slice(0, 5)`) пропом в `Footer`.
- `Footer.tsx` — остался `'use client'`, но проп `categories: TreeCategory[]` приходит уже готовым с сервера (клиентский компонент не дёргает БД сам). Ссылки — `/catalog/${cat.slug}`.
- Проверено curl'ом дев-сервера: колонка «Каталог» отдаёт реальные корневые категории (Автоаксессуары/Автозапчасти/Автосвет/Автохимия и Автокосметика/Автоэлектроника) с рабочими ссылками на дерево.

---

## Множественный выбор марки/бренда + шрифты + мелкий копирайт (2026-08-04, второй проход)

- **Множественный выбор марки/бренда** — план из предыдущей секции реализован. `BrandSelect.tsx`: строки-кнопки заменены на чекбоксы, `value`/`onChange` стали `string[]`, панель не закрывается после выбора (только по клику вне или «Сбросить выбор»). URL — `?brand=A,B` через запятую. `lib/db-catalog.ts` → `getProducts({ brand: string[] })` — `.in('brandName', brands)` вместо `.eq()`. RPC `search_products` (`scripts/add-search-function.mjs`) — `p_brand` сменил тип с `text` на `text[]`, в SQL `= any($2)` вместо `= $2`; старая сигнатура явно удалена (`drop function`) перед пересозданием, иначе `create or replace` плодит перегрузку. `app/catalog/page.tsx` и `[...path]/page.tsx` парсят `brand` из query по запятой в массив. Проверено curl'ом на живом дев-сервере: `?brand=BOSCH,TRW` даёт `4439` = `2838+1601` (сумма без пересечений), чипы снимаются по отдельности.
- **Смена шрифтов** — заказчик прислал скриншот текущего сайта: не понравился шрифт, «цифры/англ/рус разной толщины», ощущение «иишного» (шаблонного) сайта. Разобрались: причина разной толщины — IBM Plex Sans был собран из latin/cyrillic `.woff2`-подсетов разных релизов шрифта, визуально не совпадающих по насыщенности. Прогнан анализ реальных конкурентов (не по описанию — вытащили `@font-face` из их продакшн-CSS): rossko.ru → Oswald (заголовки) + Inter (текст), autodoc.ru → кастомный брендовый + Inter, autopiter.ru → Ubuntu + FiraSans. Взяли тот же дуэт что у rossko.ru (уже референс проекта по копирайтингу) — **Oswald** (заголовки, `--font-russo`) + **Inter** (текст, `--font-ibm-plex`) + **JetBrains Mono** (артикулы/коды, `--font-ibm-plex-mono`, тоже заменён по просьбе заказчика «артикулы/цены/каталог тоже меняй шрифты»). Все три — вариативные шрифты (Google Fonts CSS2 API отдаёт один файл на подсet, покрывающий весь диапазон начертаний, а не статичный файл на каждый вес) — разнобоя толщины внутри одного шрифта больше не может быть по конструкции. `next/font/local`, self-hosted, latin+cyrillic+**latin-ext**. Oswald ни разу не используется с явным весом в 45 местах кода (`font-heading`), поэтому закреплён на статичный вес 700, а не диапазон — иначе дефолтный вес просел бы до 400 и все заголовки резко похудели.
- **₽ другой толщины, чем цифры** — заметили после смены шрифтов: символ рубля рендерился системным шрифтом, тоньше соседних цифр. Причина — Google держит ₽ (U+20BD) в подсете `latin-ext`, который не был скачан (взяли только `latin`+`cyrillic`, ошибочно решив что этого достаточно). Добавлен `latin-ext` для всех трёх шрифтов, проверено через `fontTools` (`cmap` содержит `0x20BD` во всех трёх файлах).
- **Кикер-лейблы ещё раз показались «иишными»** — заказчик прислал скриншоты «ПОДБОР ЗАПЧАСТЕЙ»/«ПОМОЩЬ ЭКСПЕРТА» и указал на разреженный трекинг (`tracking-[0.2em]`) как узнаваемый штамп AI-шаблонов. Найдено 11 мест (в т.ч. переиспользуемый `SectionHeading.tsx`) — сужено до `tracking-widest` (0.1em, вдвое уже). Заказчик тем же сообщением отметил, что стиль всё ещё не нравится — нужен другой подход к самому паттерну, не просто уже трекинг (записано в `docs/AUDIT.md` и `CLAUDE.md` → «Приоритет 0.5»).
- **`font-mono` был на весь UI каталога**, не только на кодах — строка поиска, сортировка, чекбоксы фильтров, пагинация, счётчик товаров, кнопки, breadcrumb — 12 файлов. Оставлен только на артикулах, длительности услуги, количестве/суммах в корзине, бейдже корзины — остальное переведено на `font-body`.
- **Копирайт**: убрано «собственный» из «с собственным автосервисом» — заказчик попросил конкретно эту формулировку убрать. Правки в `StatsBrandsRow.tsx`, `Footer.tsx` (живые) + `HeroBanner.tsx`/`HeroBannerIcon.tsx` (черновики, не рендерятся, поправлены для консистентности).
- Заказчик продиктовал план на следующую сессию (записан в `docs/AUDIT.md`, приоритет 0.5): кикер-лейблы (стиль целиком, не трекинг), футер — неправильный список категорий (старые 7 flat-категорий вместо реального дерева), корзина — убрать оставшиеся emoji (🛒/✅ в `CartDrawer.tsx`), каталог — инлайн-степпер количества вместо повторной кнопки «В корзину», страница товара — полный редизайн + уменьшить кнопку «В корзину», отдельная страница `/cart`, убрать формулировку про адрес доставки из поля комментария в корзине.

---

## Каталог: сортировка, фильтры, разметка брендов (2026-08-04)

По пунктам аудита (`docs/AUDIT.md`, P1) и отдельным правкам по ходу сессии.

- **Сортировка каталога по умолчанию** — «в наличии → с ценой → под заказ» вместо `id asc`. `lib/db-catalog.ts` → `getProducts()`: `.order('stock', {ascending:false}).order('priceRetail', {ascending:false}).order('id', {ascending:true})` — это автоматически даёт нужные 3 группы (числовая сортировка по остатку сама разводит >0 и 0, а `priceRetail desc` внутри нулевого остатка разводит «с ценой» и «под заказ»). Та же логика — в SQL-функции `search_products` (`scripts/add-search-function.mjs`, теперь принимает `p_in_stock`/`p_price_min`/`p_price_max`, старую 5-аргументную сигнатуру явно `drop function` перед пересозданием — иначе `create or replace` плодит перегрузку). Понадобился новый индекс `Product_isActive_stock_priceRetail_idx` (`scripts/add-stock-sort-index.mjs`, `CONCURRENTLY`) — без него сортировка по 280k строк без индекса тайм-аутила (`canceling statement due to statement timeout`), это реально ловили на живом дев-сервере после первого деплоя сортировки.
- **Фильтры** — «Только в наличии» (`inStock`), цена от/до (`priceMin`/`priceMax`), марка/бренд (`brand`, уже был, но `app/catalog/[...path]/page.tsx` вообще не читал этот параметр — починили). Все фильтры комбинируются, сохраняются при пагинации и смене сортировки, есть «Сбросить фильтры».
- **Классификация `Product.brandName`.** Поле в БД плоское — не различает «марка авто» и «бренд запчасти» (см. ниже, сессия 2026-08-02). Прогнан аудит: 4646 уникальных значений всего, 461 — с 100+ товарами (82% всех «брендированных» товаров). Внутри топ-461 обнаружился настоящий мусор: «Россия»/«КИТАЙ» (страна, не бренд), названия городов («Тольятти», «Балаково»), материалы («Пластик», «Полиуретан»), технический мусор («------------», «Уценка»). Всё это вручную разобрано на 4 типа — МАРКА (54)/БРЕНД (147)/ИСКЛЮЧИТЬ (15)/СЕРАЯ_ЗОНА (245, заводы-агрегатчики типа ЗМЗ/ЯМЗ/БелЗАН и региональные дистрибьюторы — заказчик доразметит сам) — сохранено в `docs/brand-classification.tsv`. `lib/categories.ts`: `ALL_VEHICLE_MAKES`/`ALL_PART_BRANDS` вместо старого хардкода на 10 значений, `VEHICLE_MAKE_BRANDS` (Set для подписи бейджа «Марка»/«Бренд») теперь выводится из `ALL_VEHICLE_MAKES`. Главная (`StatsBrandsRow`) сознательно осталась на компактном топ-4/топ-8 (`.slice(0, N)`) — полный список только в фильтре каталога.
  - **На случай нового синка из 1С**: неразмеченный/новый бренд ничего не ломает (фильтр `?brand=X` и ссылка с карточки товара работают для любого значения, бейдж по умолчанию подписывается «Бренд» — безопасный дефолт), просто не попадёт в списки, пока не размечен. `scripts/audit-brand-classification.mjs` — сверяет живую БД с `docs/brand-classification.tsv`, печатает только то, чего там ещё нет. Не одноразовый, как остальные скрипты в папке — перезапускать вручную после крупных синков.
- **`components/ui/BrandSelect.tsx`** — новый компонент, нативный `<select>` на 200+ опций (неюзабельно длинный список) заменён на выпадающую панель с полем поиска (живая фильтрация по подстроке), группы «Марки техники»/«Бренды запчастей» сохранены через `<optgroup>`-подобную разметку div'ами.
- **Множественный выбор марки/бренда** ✓ реализовано тем же днём вторым проходом — см. следующую секцию.

---

## Карточка товара + мелкий UX (2026-08-04)

- **Бренд на карточке товара** (`app/product/[article]/page.tsx`) — был мелкой красной плашкой (`text-xs`, `bg-[#C8102E]/10`) рядом с заголовком, неудобно читать. Стал кликабельным кикером над `<h1>` (`text-xl md:text-2xl font-bold`, ссылка на `/catalog?brand=...`), красный только на hover.
- **Гарантия** — добавлен блок с нейтральным текстом («На все товары действует гарантия») без придуманных сроков; первая версия ссылалась на «гарантию производителя для ГАЗ/УАЗ/ЗМЗ» — убрано по просьбе заказчика.
- **Фото товара** — колонка сужена с половины экрана (`md:grid-cols-2`) до фиксированных 380px (`md:grid-cols-[380px_1fr]`), на мобиле `max-w-sm` вместо растяжения на всю ширину — большинство товаров всё равно без реального фото (плейсхолдер), не было смысла в такой большой площади.
- **Бренд на карточках каталога** (`ProductCard.tsx`) — красная плашка поверх фото убрана, бренд теперь жирным текстом рядом с артикулом в теле карточки.
- **«Под заказ»** — был на сером фоне (`bg-bg-muted`), неразличим на глаз рядом с ярким зелёным «В наличии». Переведён на янтарный (`bg-amber-100`/`dark:bg-amber-900/60`) везде: сетка каталога, список, карточка товара.
- **Иконки** — гаечный ключ был единственной «сервисной» иконкой везде (6 разных мест). Новый `lib/service-icons.tsx` — иконка по группе услуги (масло/диагностика/ходовая/двигатель/электрика) для `ServiceSection`/`ServiceCard`. Остальные места — по смыслу конкретного блока: «Собственный сервис» на `/about` → геометка (акцент на «один адрес»), «Автосервис» в `/contacts` → иконка автомобиля, черновые hero-баннеры (`HeroBanner`/`HeroBannerIcon`, не рендерятся) → галочка-бейдж.
- **Копирайтинг сервиса** — фраза «ремонт ГАЗ, УАЗ, ВАЗ, КАМАЗ» была везде в текстах сервиса, но КАМАЗ не входит в зону обслуживания сервиса (см. `CLAUDE.md` → «ОГРАНИЧЕНИЯ СЕРВИСА»). Заменено на «отечественные авто и иномарки» — `ServiceSection`, `app/service/page.tsx` (meta + subtitle), абзац «Кто мы» на `/about`.
- **«Доставка» убрана везде по тексту сайта** — заказчик попросил, условия ещё не готовы объявлять. Убрано из живого слайда `HeroSlider`, SEO-описания `/catalog`, и из неиспользуемых черновиков (`HeroSection`, `TopBar`, `AdvantagesSection` — там же убрана целиком карточка «Быстрая доставка», сетка преимуществ пересчитана с 4 на 3 колонки). Поле «Адрес доставки» в `CartDrawer` (плейсхолдер текстового поля заказа) не трогали — это не маркетинговое обещание, а рабочее поле формы.
- Мелкий фикс инфраструктуры: `.env.local` — `DIRECT_URL` был испорчен лишним символом сразу после закрывающей кавычки, `sslmode=disable` из-за этого не распознавался, скрипты с прямым подключением к Postgres падали по TLS (`self-signed certificate in certificate chain`).

---

## Логотип для тёмных фонов (2026-08-03)

Логотип в хедере при переключении в тёмную тему перекрашивался CSS-фильтром (`dark:brightness-0 dark:invert`) в белый силуэт — по просьбе заказчика убрано. Вместо фильтра — реальный полноцветный логотип заказчика (синий/красный «T» + белая надпись «ТРАК»), присланный PNG с чёрным фоном без альфа-канала; фон вырезан в прозрачность через Pillow (`public/logo-dark.png`).
- `Header.tsx` — два `<Image>` вместо одного с фильтром: `/logo.png` (`dark:hidden`) и `/logo-dark.png` (`hidden dark:block`)
- `Footer.tsx` — футер всегда на синем `#1A3A6B`, там тоже был фильтр (`brightness-0 invert`) вместо цветного логотипа — заменён на `/logo-dark.png` напрямую

---

## Аудит документации (2026-08-03)

Полная сверка `CLAUDE.md` + `docs/*.md` + `.env.example` с реальным кодом. Найдено и исправлено:
- `docs/BRIEF.md` — «магазин: ВАЗ/ГАЗ/УАЗ/КАМАЗ» противоречило собственному аудиту БД в `CLAUDE.md` («не сужать до 4 марок») — исправлено
- `docs/INTEGRATIONS.md` — Яндекс.Карты указаны в удалённом `ContactsSection.tsx` — исправлено на `app/contacts/page.tsx`
- `docs/ARCHITECTURE.md` — порядок секций главной устарел (описывал состав до редизайна) — обновлён на актуальный
- `docs/DESIGN.md` — верхний блок CSS-переменных (`--red`, `--black`...) давно не существует в `globals.css`, вытеснен семантическими токенами — блок убран
- `CLAUDE.md` — не хватало `components/ui/Breadcrumb.tsx` и `lib/prisma.ts` в списках; кеш дерева категорий описан устаревшим способом (`_treeCache`) вместо реального `unstable_cache`/`invalidateCategoryTree()` — добавлено/исправлено
- `.env.example` — не совпадал с реальными env (не было `NEXT_PUBLIC_SUPABASE_*`/`DIRECT_URL`/`SYNC_LOGIN`/`SYNC_PASSWORD`, был несуществующий `SYNC_SECRET_KEY`, упоминание Netlify вместо Vercel) — переписан

---

## План: реконструкция главной по аудиту адаптива + идеям rossko.ru (2026-08-01)

Провёл аудит адаптива главной (Playwright MCP, 375/768/1440px) и разбор структуры секций. В этой же сессии уже исправлены 2 бага: `HeroSlider` — стрелки навигации перекрывали заголовок на мобильном (`hero-prev`/`hero-next` → добавлен `hidden sm:flex`), `ServiceSection` — название услуги обрезалось `truncate` посередине слова (убран класс, текст переносится). Ниже — план на следующие сессии, идти по порядку сверху вниз.

### Очередь (по одному пункту за сессию)
1. ~~**Header**~~ ✓ (2026-08-01) — добавлен `<nav>` в десктоп-панель `components/layout/Header.tsx` (Каталог/Сервис/О нас/Контакты, `hidden lg:flex`, между логотипом и строкой поиска), пункт «Контакты» добавлен и в мобильное меню (общий массив `navLinks`). Проверено Playwright MCP на 1440/1024/768/375px — нигде не теснит поиск/CTA, на md (768) nav скрыт вместе с остальным desktop-блоком, мобильное меню открывается корректно.
2. ~~**CategoryNavTabs**~~ ✓ (2026-08-01) — переведён на реальное дерево категорий: `app/page.tsx` вызывает `getCategoryTree()` рядом с существующим `getFeaturedProducts()` и передаёт `categories` пропом (тот же паттерн, что `ProductsSection`). Компонент стал `'use client'`, ссылки `/catalog/${slug}` на все 15 корневых категорий. Пункт «Услуги СТО» убран по просьбе заказчика (это не категория каталога). Добавлен `embla-carousel-react` (`dragFree`, без loop/autoplay) со стрелками-кнопками в стиле `ReviewsSection`, дизейблятся на краях через `canScrollPrev`/`canScrollNext`, `hidden sm:flex` — на мобиле обычный тач-скролл. Проверено Playwright MCP на 1440px (скролл работает, стрелки переключаются) и 375px (стрелок нет, скролл тачем).
3. ~~**StatsBrandsRow**~~ ✓ (2026-08-02) — марки авто и бренды запчастей разделены на два подписанных блока («Марки авто» / «Бренды запчастей»), под статистикой добавлена трастовая строка (официальный представитель ГАЗ, субдилер УАЗ, 30 лет на рынке). Проверено через accessibility-снапшот (визуальный скриншот не снялся — Playwright MCP завис на этой сессии независимо от изменений, зависание воспроизвелось и на `/catalog`, куда правки не касались).
   - **Список брендов пересчитан по факту:** посчитал частоту `Бренд` по `products.csv` (280k строк). Марки авто (ГАЗ/УАЗ/ВАЗ/КАМАЗ) подтвердились как реальный топ-4. Бренды запчастей расширены до 6: узнаваемые (BOSCH, MANN, TRW) + реально самые частые в каталоге (FEBEST, TRIALLI, FENOX) — по просьбе заказчика, с прицелом на SEO.
   - **Бейджи стали рабочими ссылками на `/catalog?brand=...`** (SEO-эффект: реальные внутренние ссылки на отфильтрованный каталог, а не просто текст). `lib/db-catalog.ts` — `getProducts()` получил параметр `brand` (точный фильтр по `Product.brandName`). `app/catalog/page.tsx` — `generateMetadata()` вместо статичного `metadata`, при `?brand=X` отдаёт уникальные `title`/`description` («Запчасти BOSCH — купить в Уфе | ТРАК»). `CatalogView.tsx` — проп `brand`, заголовок «Запчасти {brand}», фильтр сохраняется в URL при пагинации/поиске/сортировке, снимаемый бейдж-индикатор активного фильтра.
   - **Важное ограничение нашли по ходу:** `Product.brandName` — одно плоское поле в БД, оно не различает «марка авто» (КАМАЗ) и «бренд запчасти» (BOSCH) — это буквально то же самое значение из колонки «Бренд» в выгрузке 1С. Таблицы `Brand`/`CarModel`/`ProductCompatibility` под нормальную структуру существуют в `prisma/schema.prisma`, но пустые и ничем не заполняются (см. Приоритет 2 ниже). Разделили только на уровне подписи бейджа: `lib/categories.ts` → `VEHICLE_MAKE_BRANDS` (Set из ГАЗ/UAZ/LADA/KAMAZ — те же значения `brandName`, что и марки в БД, не кириллические витринные лейблы), `CatalogView.tsx` подписывает активный фильтр «Марка: KAMAZ» либо «Бренд: BOSCH» по этому списку. В марки авто (`StatsBrandsRow.carBrands`) добавлено поле `dbBrand` — реальное значение в БД (UAZ/LADA/KAMAZ латиницей, ГАЗ кириллицей), отдельно от `label` (витринная кириллица ВАЗ/УАЗ/КАМАЗ).
4. ~~**CategoriesSection**~~ ✓ (2026-08-03) — убрана с главной (`app/page.tsx`): импорт и `<CategoriesSection />` удалены. Дублировала категории из `CategoryNavTabs` (п.2) + в `/catalog` уже есть полноценная сетка (`CategoryTiles`). Файл `components/sections/CategoriesSection.tsx` не удалён — оставлен неиспользуемым, как и другие черновые секции. Проверено Playwright MCP: секция пропала, порядок остальных секций (`CategoryNavTabs` → `HeroSlider` → `StatsBrandsRow` → `ProductsSection` → `ServiceSection` → `ReviewsSection`) не нарушен.
5. ~~**ProductsSection → замена**~~ ✓ (2026-08-03) — заменена на блок **«Эксперт поможет подобрать запчасть»**. Новые файлы: `components/sections/PartFinderCTA.tsx` и `components/ui/PartRequestModal.tsx` (модалка по образцу `BookingModal`: маска телефона/rate-limit/honeypot из `lib/phone-utils.ts`, поля «какая деталь нужна» (textarea, обязательно) + телефон (обязательно) + марка/модель авто (опционально)). `/api/booking` расширен: `name` стал опциональным, добавлено опциональное поле `part`, текст в Telegram различается («Заявка на подбор запчасти» vs «Новая заявка»), новый роут не создавался. `app/page.tsx`: `ProductsSection`/`getFeaturedProducts` убраны, `PartFinderCTA` встал на их место. `components/sections/ProductsSection.tsx` не удалён, оставлен неиспользуемым.
   - **Финальный вид — тёмный баннер в стиле rossko.ru** (референс заказчика): `rounded-2xl bg-[#161616]`, слева фото эксперта на всю высоту карточки (`object-cover object-top`), справа заголовок-вопрос + подпись + кнопка/телефон. Фото — реальное, сгенерировано заказчиком через GPT (`public/images/partfinder-expert.png`, 1254×1254, тёмный фон с красным акцентным бликом под бренд-цвет). Шов между фото и плоским фоном карточки убран градиентной маской-blend (`linear-gradient` transparent→`#161616`, `to_bottom` на мобиле / `to_right` на десктопе).
6. **ServiceSection** — без изменений, признан рабочим.
7. ~~**ContactsSection global duplicate**~~ ✓ (2026-08-03) — `<ContactsSection />` убран из `app/layout.tsx` (рендерился на каждой странице поверх Footer, дублировал полноценную `/contacts`). Компонент нигде больше не использовался — `components/sections/ContactsSection.tsx` удалён (в отличие от других неиспользуемых секций, здесь именно удаление, как и планировалось). Проверено Playwright MCP: на `/catalog` дубль пропал, `/contacts` работает как прежде.

### Очередь пройдена полностью (п.1–7) — план реконструкции главной закрыт 2026-08-03

### Идеи rossko.ru про запас (не в этой очереди, добавлять постепенно)
- УТП-блок: тёмная секция, фото человека + 6 кликабельных плашек преимуществ («Запчасти всегда в наличии», «Гарантия», «Помощь экспертов 24/7» и т.д.) — переосмыслить неиспользуемый `AdvantagesSection.tsx` в этом стиле.
- Промо-слайд со скидкой/акцией — `HeroSlider` уже поддерживает несколько слайдов, добавить как отдельный слайд когда появится повод (акция, сезонное предложение).
- Квик-фильтры подкатегорий пилюлями в `/catalog/[...path]` (по типу товара внутри категории, не только по бренду) — отдельная задача, не по главной.
- **Не берём**: VIN-поиск (нужна база совместимости VIN↔деталь), real-time слоты самовывоза («заберите сегодня с 18:40»), мобильное приложение, доставка по 158 городам — завязано на инфраструктуру федеральной сети rossko, не масштаб одного магазина в Уфе.

### Как продолжать
Брать пункты 1→7 по одному за сессию (можно быстрее, если позволяет объём). После каждого — обновлять этот раздел (вычёркивать сделанное) и сверяться с `CLAUDE.md` (актуализировать список компонентов, если структура секций на главной меняется).

---

## Hero сузили + Playwright MCP — TODO на следующую сессию (2026-08-01)

- `components/sections/HeroSlider.tsx` — по просьбе заказчика hero сделан узким баннером вместо fullscreen (90vh → 420px фиксированной высоты), CTA/заголовки/паддинги уменьшены под новую высоту. Слайдер тот же (Swiper, fade, реальные фото фоном, автоплей)
- **Баг и фикс:** после десятков правок за сессию Swiper переставал инициализироваться (`.swiper.swiper` — `undefined`, класс `swiper-initialized` не навешивался, слайды рендерились друг под другом сплошным списком вместо fade-подложки одного поверх другого). Ошибок в консоли не было — похоже на битое состояние Fast Refresh для библиотеки, которая работает с DOM в обход React. Помог чистый рестарт `next dev` (+ `rm -rf .next`)
- **Не перепроверено визуально** после рестарта — нужно в начале следующей сессии открыть `/` и убедиться, что слайдер листает по одному слайду, а не стопкой
- **Playwright MCP подключён** (`claude mcp add playwright -- npx -y @playwright/mcp@latest`, scope local, статус ✔ Connected по `claude mcp list`) — тулы появятся только в новой сессии (mid-session регистрация MCP не подхватывается). Дальше использовать его вместо Claude in Chrome для визуальных проверок
- **Мобильная адаптивность не проверена** — новые `TopBar` + строка поиска в `Header` + `CategoryNavTabs` не смотрели на узком экране (`resize_window` в Claude in Chrome не сработал в этой сессии — окно не уменьшилось)
- **`ContactsSection` дублируется глобально** (`app/layout.tsx:98`, рендерится на каждой странице поверх Footer) — решение отложено, нужно спросить заказчика: убирать или оставить
- **График работы** — заказчик обещал прислать точный по дням, пока в `Footer.tsx`/JSON-LD старые часы-заглушки (Пн–Пт 9:00–20:00, Сб–Вс 9:00–17:00 магазин; Пн–Вс 9:00–20:00 сервис)

---

## Редизайн главной под мокап «Светлый каталожный» (2026-08-01)

Заказчику нужен более «дорогой» визуал — сделали 3 варианта через Claude Design (claude.ai/design), выбрали светлый каталожный, переносим на сайт.

- `app/page.tsx` — новый порядок секций: `CategoryNavTabs` → `HeroSlider` → `StatsBrandsRow` → `CategoriesSection` → `ProductsSection` → `ServiceSection` → `ReviewsSection`. `AdvantagesSection` и `ServiceGallery` больше не рендерятся на главной (файлы не удалены, как и другие неиспользуемые секции)
- `components/layout/TopBar.tsx` — новый, тонкая полоса над Header (доставка + телефон), фикс на всех страницах
- `components/layout/Header.tsx` — добавлена строка поиска (десктоп: в шапке; мобилка: в выпадающем меню), ведёт на `/catalog?q=...`. Header сдвинут на `top-9` под TopBar
- `app/layout.tsx` — глобальный отступ под контент увеличен с `pt-16` до `pt-[100px]` (TopBar 36px + Header 64px)
- `components/sections/CategoryNavTabs.tsx` — новый, вкладки быстрого перехода по `STATIC_CATEGORIES` + «Услуги СТО», только на главной
- `components/sections/StatsBrandsRow.tsx` — новый, реальные цифры (30+ лет / 50 000+ позиций / без выходных) + бейджи марок/брендов
- `components/sections/CategoriesSection.tsx` — вёрстка сеткой из плиток (иконка сверху, название снизу) вместо строк списка
- `components/sections/ServiceSection.tsx` — карточки услуг переверстаны в компактные строки (иконка · название · длительность · цена · кнопка), группы-вкладки оставлены
- `lib/categories.ts` — `SEARCH_TERM` перенесён сюда из `CategoriesSection.tsx` (был `export` из `'use client'`-файла — RSC не даёт импортировать значение клиентского модуля в серверный компонент)
- **Hero:** пробовали кастомный баннер с иллюстрацией машины (сначала тонкий контур, потом плоская заливка) — заказчику не понравился ни один вариант. По итогу вернули как было — `HeroSlider` с реальными фото на фоне (`hero-1..3.webp`). Черновые варианты остались неиспользуемыми: `components/sections/HeroBanner.tsx` (фото), `components/sections/HeroBannerIcon.tsx` (контурная иконка)
- **Найдено при проверке в браузере:** `app/layout.tsx` рендерит `<ContactsSection />` глобально на каждой странице (между контентом и Footer) — раньше в `CLAUDE.md` было написано, что компонент не используется. По факту дублирует `/contacts` на каждой странице сайта, включая каталог и карточки товара. Не трогали — отдельное решение нужно от заказчика
- **Уточнение по ассортименту:** в БД проверили реальные бренды/категории — магазин продаёт не только ГАЗ/ВАЗ/УАЗ/КАМАЗ, а также МАЗ/ПАЗ/УРАЛ/тракторы/спецтехнику/грузовые иномарки + сторонние бренды (BOSCH, FEBEST, TRW, MANN и др.), плюс масла/автохимию/лампы/коврики. Тексты на главной поправлены под это, `CLAUDE.md` тоже обновлён

---

## Тесты: старт (2026-08-01)

- Поставлен Vitest (`vitest.config.mts`, скрипт `npm run test`). Тестов раньше не было вообще
- Первые тесты — `lib/phone-utils.test.ts` (9 тестов): форматирование по мере набора номера, конвертация 8→7, обрезка лишних цифр, нормализация в `+7XXXXXXXXXX`, валидация длины
- **Попутно нашёл и починил дублирование:** `components/ui/BookingModal.tsx` держал свою локальную копию `formatPhone`/`normalizePhone`/`isPhoneValid` вместо импорта из `lib/phone-utils.ts` (сейчас была идентична, но это была точка расхождения — `CLAUDE.md` ошибочно утверждал, что обе формы уже переиспользуют общий код). Теперь обе формы (`BookingModal`, `CartDrawer`) используют один и тот же протестированный код
- Следующие цели: `/api/order`, `/api/booking` (zod-валидация, honeypot, серверная переоценка цены — самые критичные пути по аудиту)

---

## Фиксы по итогам аудита (code-review + code-auditor, 2026-08-01)

- `next.config.mjs` — `redirects()`: 7 старых мёртвых категорий (`dvigateli`, `filtry` и т.д.) → `/catalog` (308, permanent)
- `app/catalog/[...path]/page.tsx` — если сегмент URL не резолвится в категорию и путь ≥2 сегментов, пробуем найти товар по последнему сегменту (`getProductByArticle`) и редиректим на `/product/[article]` — закрывает старые ссылки `/catalog/[slug]/[article]` без хардкода паттернов
- `lib/db-catalog.ts` — дерево категорий переведено с голой module-level переменной (`_treeCache`, живёт только в рамках одного serverless-инстанса) на `unstable_cache` с тегом `categories` — инвалидация теперь работает across всех инстансов. Добавлен `invalidateCategoryTree()`, вызывается из `app/api/products/route.ts` после успешного синка от 1С
- `lib/db-catalog.ts` — явный `.limit(5000)` на оба запроса категорий (было без лимита — PostgREST молча обрезает на дефолтных ~1000, а категорий уже 927)
- **Поймали и починили баг в процессе:** обёртка `unstable_cache` кеширует и ошибочный результат, если функция не throw'ает. При первом же транзиентном таймауте БД (`57014 canceling statement due to statement timeout` на JOIN Category↔Product) пустое дерево закешировалось намертво — все категории отдавали 404. Функция теперь `throw`-ит на ошибке вместо `return {cats:[],...}`, `loadTree()` ловит исключение и деградирует как раньше (retry на следующий запрос, а не постоянный пустой кеш)

## Вторая волна фиксов по аудиту (2026-08-01)

- `lib/db-catalog.ts` — фильтр товаров по ветке категорий (`getProducts({categoryPath})`) был `Category.path LIKE 'X%'` без границы `/` после префикса: теоретически id одной категории мог оказаться строковым префиксом id другой (проверял вживую — коллизий на текущих 927 id нет, но гарантии не было). Заменено на `path.eq.X OR path.like.X/*` — закрывает границу полностью
- `lib/db-catalog.ts` — `getCategoryNode()`/`getCategoryTree()` теперь различают «категории нет» (null, дерево загрузилось) и «БД недоступна» (throw). `/catalog` при сбое молча прячет плитки (как раньше), `/catalog/[...path]` при сбое показывает «каталог временно недоступен» вместо жёсткого 404 — раньше сбой БД выглядел как несуществующая страница
- `app/api/products/route.ts` — `getCategoryId()` кешировал только успешные попадания по `Код_Каталога`; товар с неизвестным/пустым кодом дёргал Prisma заново на каждой строке синка с этим кодом. Теперь кешируется и фолбэк
- `scripts/generate-import-csv.mjs` — единственное оставшееся место со старой keyword-категоризацией по названию (расходилась с реальной по `Код_Каталога`). Переведено на ту же логику, что и остальные импорт-скрипты — подгружает карту `externalId → categoryId` одним запросом в начале
- `next-auth` — проверил, нигде не импортируется, но **не удалял**: в `CLAUDE.md` явно прописан в разделе «СТЕК (не обсуждается)», значит это декларированный выбор под будущую авторизацию, а не случайный мёртвый код. Оставлено как есть
- Настроен ESLint (`.eslintrc.json`, `next/core-web-vitals`) — раньше `npm run lint` не работал вообще, конфига не было. Нашлось одно предупреждение (`react-hooks/exhaustive-deps` на дебаунсе поиска в `CatalogView.tsx`) — намеренно подавлено с комментарием, добавление зависимостей сломало бы дебаунс

## Реальное дерево категорий 1С + иерархический каталог (2026-08-01)

### Категории из справочника 1С

- Получен `КаталогиСайт.txt` — официальный справочник категорий от 1С: 920 категорий, иерархия до 4 уровней (`Код` / `КодРодитель` / `Наименование`)
- `prisma/schema.prisma` — добавлено поле `Category.externalId` (уникальное, хранит `Код` из справочника)
- `scripts/import-categories.mjs` — новый скрипт, парсит `КаталогиСайт.txt` и заливает полное дерево в `Category` (path/level/parentId по иерархии). Идемпотентен (upsert по `externalId`)
- `app/api/products/route.ts` — категоризация товаров теперь по `Код_Каталога` (2-е поле CSV от 1С, раньше игнорировалось) через `Category.externalId`, вместо угадывания по ключевым словам в названии. Неизвестный/пустой код → категория «Прочее»
- `scripts/import-products.mjs` — тот же принцип категоризации (для одноразовых импортов из `products.csv`)
- `lib/categories.ts` — удалена keyword-детекция (`detectCategorySlug`, `CATEGORY_KEYWORDS`), она больше не нужна. `STATIC_CATEGORIES` оставлен — используется только для витрины на главной (`CategoriesSection`)
- **Существующие 280k товаров перекатегоризированы** — прогнан свежий `products.csv` через обновлённый `scripts/import-products.mjs` (813 реальных категорий теперь используются, старые 7 плоских полностью опустели). Нашли и исправили баг: `ON CONFLICT DO UPDATE SET` в SQL не включал `"categoryId"`, из-за чего первый прогон отчитался об успехе, но категорию не менял — поймали только сверкой живых данных, не по логу скрипта

### Иерархический каталог (замена плоского таб-бара)

- `lib/db-catalog.ts` — `getCategoryTree()` / `getCategoryNode(slug)`: дерево строится 2 запросами, кешируется в памяти модуля, `hasProducts` считается рекурсивно вверх (категория «жива», если у нее или у потомка есть товар). `getProducts()` фильтрует по префиксу `Category.path` — сразу вся ветка, не только точная категория
- `app/catalog/page.tsx` — плитки 15 корневых категорий + общий поиск по всем товарам
- `app/catalog/[...path]/page.tsx` — новый catch-all вместо `[slug]/page.tsx`, работает на любую глубину дерева: хлебные крошки + плитки подкатегорий + товары всей ветки
- `app/catalog/CategoryTiles.tsx`, `components/ui/Breadcrumb.tsx` — новые переиспользуемые компоненты
- Товар переехал с `/catalog/[slug]/[article]` на `/product/[article]` — освобождает роут категорий от коллизий с catch-all, артикул и так глобально уникален
- `components/sections/CategoriesSection.tsx` (главная) — 6 плиток вели на старые 7 slug, которые опустели после перекатегоризации → переведены на поиск по ключевому слову (`/catalog?q=...`) вместо прямой ссылки на (теперь пустую) категорию

### Мелкий фикс UX

- `app/catalog/CatalogView.tsx` — оба пустых состояния каталога («Товары появятся после первой синхронизации с 1С») заменены на нейтральное «Товары не найдены» — старая формулировка была видна покупателям на сайте

---

## Наведение порядка + документация (2026-05-25)

### Реализовано

- Убраны ~25 скриншотов из корня → перемещены в `screenshots/` (добавлен в `.gitignore`)
- Удалены: `logo.png` в корне (дубль), `utils/supabase/` (не использовался), `app/font-compare/` (dev-страница)
- Удалён мёртвый код `SearchRow` / `adaptSearch` из `lib/db-catalog.ts`
- `.gitignore` обновлён: добавлены `.playwright-mcp/`, `screenshots/`, `*.woff2`
- `app/sitemap.ts` — добавлен `/contacts` (был пропущен)
- Документация приведена в соответствие: `CLAUDE.md`, `docs/ARCHITECTURE.md`, `docs/DESIGN.md`, `docs/BRIEF.md`

---

## Каталог UX + Контакты + UX-фиксы (2026-05-23 – 2026-05-25)

### Реализовано

**Страница `/contacts` (полный редизайн):**
- `app/contacts/page.tsx` — два отдела в левой колонке, карта + адрес в правой (grid 2 колонки)
- Каждый отдел с цветным left-border strip: Магазин `#C8102E`, Автосервис `#1A3A6B`, Оптовый `#C4922A`
- Яндекс.Карты embed `map-widget/v1/org/trak/1100951090`, высота 320px
- Email / WhatsApp в блоке `bg-bg-muted` (визуально отличается от карточек отделов)
- `ContactsSection.tsx` более не используется — заменён страницей

**CatalogView — полная переработка UX (`app/catalog/CatalogView.tsx`):**
- Debounced поиск 350ms (useEffect с setTimeout). Sync от server props через отдельный useEffect
- Сортировка: `price_asc` / `price_desc` (сортировка по имени убрана — вызывала таймауты)
- Переключатель вид: сетка / список. Список — компонент `ProductListRow` (артикул + название + статус + цена + кнопка)
- `buildUrl()` — централизованный билдер URL с q/sort/page параметрами
- Кнопка «Все» захардкожена как `/catalog` (не через `buildUrl`) — исправляет баг с категорией на `basePath`
- `lib/db-catalog.ts` — параметр `sort`, поиск мультислово AND через цепочку `.or()`:
  ```ts
  for (const word of words) query = query.or(`name.ilike.%${word}%,article.ilike.%${word}%`)
  ```

**UX-фиксы главной страницы:**
- `CategoriesSection.tsx` — карточки категорий переделаны в горизонтальные строки (icon + name + chevron) вместо вертикальных карточек
- `ServiceSection.tsx` — добавлены 3 feature-пункта (checkmark-circle SVG) перед CTA: «3D развал-схождение», «Гарантия», «Без выходных»
- `ReviewsSection.tsx` + `ReviewCard.tsx` — добавлен `h-full` для выравнивания высот карточек в Embla Carousel
- `SectionHeading.tsx` — добавлен `mx-auto` для центрирования subtitle при `align="center"`

### Известные ограничения поиска
Поиск работает как substring ilike — «фильтр» найдёт «фильтры», но опечатки не обрабатываются. Fuzzy-поиск требует GIN-индекс в БД (pg_trgm) — не реализован из-за statement timeout на 280k строках без индекса.

---

## Design Review + Emoji → SVG (2026-05-22)

### Реализовано

**Иконки:**
- `lib/categories.ts` — удалено поле `icon` (было emoji)
- `lib/mock-data.ts` — удалено поле `icon` из типа `MockService` и всех 17 услуг
- `components/sections/CategoriesSection.tsx` — emoji заменены на SVG-иконки (карта `ICONS` по slug)
- `components/sections/ServiceSection.tsx` — emoji → красный SVG checkmark-circle
- `components/ui/ServiceCard.tsx` — emoji → SVG иконка инструмента
- `app/about/page.tsx` — emoji в advantages → inline SVG (медаль, склад, ключ, щит)

**Галерея сервиса:**
- `components/sections/ServiceGallery.tsx` — пути `.svg` → `.jpg`, добавлен стилизованный плейсхолдер (иконка + alt-текст) пока нет реальных фото

**Хедер:**
- Мобильная строка: убран дублирующий номер телефона `font-mono text-xs` (он есть в бургер-меню)

**SEO/Meta:**
- `app/layout.tsx` — добавлены `og:url: 'https://trak-ufa.ru'` и `og:siteName: 'ТРАК'`

### Правило на будущее
Emoji запрещены в UI. Только SVG-иконки (stroke, currentColor, viewBox 0 0 24 24).

---

## Каталог + Security + Категории из БД (2026-05-22)

### Реализовано

**Каталог через Supabase JS клиент:**
- `lib/supabase.ts` (новый) — `createClient` с кастомным fetch: `cache: no-store`, `connection: close` (предотвращает 15s stale keep-alive таймауты), удаляет `HTTPS_PROXY`/`HTTP_PROXY` при инициализации
- `lib/db-catalog.ts` — полностью переписан на Supabase JS. Prisma оставлен только для `getOrCreateCategoryId` (используется в import-скриптах). Причина: Prisma + Supavisor (порт 6543) зависал на `DEALLOCATE ALL` при второй транзакции в рамках одного PrismaClient на Windows — нерешаемо без смены хостинга
- `next.config.mjs` — удаляет proxy env vars при старте; задаёт дефолты для `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
- `app/catalog/page.tsx` — `getProducts` + `getCategories` через `Promise.all`
- `app/catalog/[slug]/page.tsx` — то же + allowlist валидация `params.slug` (→ 404 для неизвестных)
- `app/catalog/CatalogView.tsx` — принимает `categories: DbCategory[]` как prop вместо хардкода `STATIC_CATEGORIES`

**Категории из БД:**
- `getCategories()` в `lib/db-catalog.ts` — fetches из Supabase с module-level кешем
- `DbCategory` тип экспортируется из `lib/db-catalog.ts`
- Отображаются в табах каталога динамически; добавление категории в Supabase → подхватывается автоматически

**Security hardening (коммит d7def04):**
- `app/api/products/route.ts`, `app/api/orders/route.ts` — `checkBasicAuth`: `===` → `timingSafeEqual` из `node:crypto`
- `lib/db-catalog.ts` — `sanitizeSearch()`: вырезает `(),"'\` из поискового запроса перед передачей в PostgREST `.or()` фильтр
- `app/catalog/[slug]/page.tsx` — allowlist `VALID_SLUGS` перед запросом в БД
- `app/api/orders/route.ts` — исправлен `checkBasicAuth` (тот же паттерн что в products)

**RLS:**
- `public.Brand` — включён RLS (ранее был единственной незащищённой таблицей)

**Производительность каталога (локально):**
- Cold start первый запрос: ~5-7s (DNS + TCP handshakes)
- Последующие: 0.5-1s
- На Vercel (co-located с Supabase eu-west-2) будет быстрее

### Нерешённые проблемы
- **`NEXT_PUBLIC_SUPABASE_URL`** и **`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`** нужно добавить в `.env.local` (если не заданы — дефолты берутся из `next.config.mjs`, что нормально для локальной разработки)

---

## Загрузка каталога + интеграция 1С (2026-05-20)

### Реализовано

- **`products.csv`** — получен от 1С разработчика: 280 072 товара, формат `Код;Код_Каталога;...` (заголовок `;`, данные `\t`)
- **`scripts/generate-import-csv.mjs`** — генератор CSV для импорта через Supabase Dashboard (трансформирует products.csv → формат таблицы Product)
- **`scripts/import-products.mjs`** — скрипт прямого импорта через pg/Supabase JS (для будущего использования когда будет стабильное соединение)
- **`utils/supabase/`** — клиент-хелперы (server.ts, client.ts, middleware.ts) для работы с Supabase JS
- **`app/api/products/route.ts`** — исправлен парсер CSV: теперь определяет разделитель автоматически (`\t` или `;`)
- **Категории в БД** — 7 категорий созданы: dvigateli, filtry, tormoznaya-sistema, podveska, masla-i-zhidkosti, transmissiya, prochee
- **Импорт** — 280к товаров загружены через Supabase Dashboard (Table Editor → Import CSV)

### Структура products.csv

| Колонка | Название | Пример |
|---|---|---|
| 0 | Код (externalId) | `000086047` |
| 1 | Код_Каталога | `000000688` |
| 2 | Код_Поставщика | `50515632` |
| 3 | Наименование | `Диск тормозной` |
| 4 | Артикул | `PBD2753` |
| 5 | Бренд | `PATRON` |
| 6 | Цена1 | `0` |
| 7 | Остаток | `0` |

### Известные ограничения

- **Категории** — сейчас 7 (по ключевым словам в названии). Реальных категорий в 1С — 813 `Код_Каталога`, маппящихся на ~15 групп как на trak-ufa.ru. **Ждём от 1С справочник категорий** (`Код_Каталога → Название`).
- **Подключение к БД локально** — `DATABASE_URL` использует transaction pooler (порт 5432 session mode). Прямое соединение через `pg` нестабильно из-за особенностей локальной сети. Импорт через Supabase Dashboard работает надёжно.
- **`DIRECT_URL`** — обновлён на прямой URL Supabase (db.scprbpqwugshqbttbowe.supabase.co:5432)

### ID категорий в БД

| Slug | ID |
|---|---|
| dvigateli | cmp9tvhvf0000hae5xtc3hkzr |
| filtry | cmp9tvjdm0003hae5h3nfkkzm |
| masla-i-zhidkosti | cmpcznfz5047dz0y |
| podveska | cmp9tvmru000bhae5gz7yrlvg |
| prochee | cmp9tvkvw0006hae5j9jjx1wo |
| tormoznaya-sistema | cmpcznfjkfqjxeo4 |
| transmissiya | cmpczng409hstnkg |

---

## Следующие шаги (приоритет)

### 1. Проверить каталог на сайте
После завершения импорта: `npm run dev` → `/catalog` — убедиться что товары отображаются.

### 2. Задеплоить на Vercel
Добавить в Vercel Dashboard → Settings → Environment Variables:
- `DATABASE_URL` — session pooler URL (уже должен быть)
- `DIRECT_URL` — прямой URL Supabase
- `NEXT_PUBLIC_SUPABASE_URL` = `https://scprbpqwugshqbttbowe.supabase.co`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` = `sb_publishable_588VWvqjtvBQoWcqy2aSuQ_We17-S4S`
- `SYNC_LOGIN`, `SYNC_PASSWORD` — для 1С API
- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `TELEGRAM_CHAT_ID_2`
- **НЕ ставить** `HTTPS_PROXY`, `HTTP_PROXY` — сломают сборку

### 3. FTP-синхронизация (GitHub Actions)
Текущая интеграция с 1С работает через FTP: 1С выгружает `products.csv` на FTP-сервер.
Нужно создать `.github/workflows/sync-products.yml`:
- Запускается по расписанию (cron, раз в сутки или чаще)
- Скачивает `products.csv` с FTP
- Прогоняет через логику категоризации
- Делает upsert в БД через `pg` (на GitHub Actions сеть стабильная)
- Использует те же env vars что и сайт

### 4. Категории от 1С (ждём)
Когда придёт справочник `Код_Каталога → Название`:
- Обновить `lib/categories.ts` — заменить 7 категорий на реальные (~15 как на trak-ufa.ru)
- Написать SQL для перекатегоризации товаров в БД
- Обновить UI каталога (вкладки/фильтры)

### 5. Переезд с Vercel на другой хостинг
Сайт будет переезжать на другой хостинг и домен.
- GitHub Actions cron не зависит от хостинга — переносить не нужно
- FTP worker останется рабочим
- Нужно обновить `NEXTAUTH_URL` и `vercel.json` → конфиг нового хостинга

---

## Security fixes (2026-05-17)

### Исправлено

- `app/api/orders/route.ts` — CSV injection: все поля экранируются через `escapeCsv()` (защита от формул в Excel)
- `app/api/order/route.ts` — price tampering: цена берётся из `Product.priceRetail` по артикулу, клиентская цена используется только если товар не найден в БД
- `app/api/order/route.ts`, `app/api/booking/route.ts` — Telegram injection: убран `parse_mode: 'Markdown'`, сообщения отправляются plain text
- `app/api/products/route.ts`, `app/api/orders/route.ts` — Basic Auth: `split(':')` заменён на `indexOf(':')` (корректная обработка паролей с двоеточием)
- `lib/prisma.ts` — query logging отключён в production (утечка PII в логи Netlify)

---

## Supabase + подключение БД к Netlify (2026-05-17)

### Реализовано

- **Supabase** — создан проект, схема задеплоена через `prisma db push`
- **Connection pooler** — переключились на Supavisor (Transaction mode, порт 6543) для стабильной работы Prisma в serverless
- `prisma/schema.prisma` — добавлен `directUrl = env("DIRECT_URL")` и `binaryTargets = ["native", "rhel-openssl-3.0.x"]` для работы на Netlify (Linux)
- `.env.local` — `DATABASE_URL` (pooler), `DIRECT_URL` (прямое соединение), `SYNC_LOGIN`, `SYNC_PASSWORD`
- **Netlify env** — добавлены `DATABASE_URL`, `DIRECT_URL`, `SYNC_LOGIN`, `SYNC_PASSWORD`
- Импорт CSV протестирован локально — 9/9 строк без ошибок

### Данные для специалиста 1С
- **URL:** `https://trak-website.netlify.app/api/products`
- **Метод:** POST, тело — CSV с разделителем `;`
- **Auth:** Basic Auth (логин/пароль — в `.env.local`)

### Следующие шаги
1. **Первая синхронизация** — 1С делает POST с реальным прайсом, проверяем каталог
2. **Фото товаров** — решить как быть без изображений из 1С (заглушка или Supabase Storage)
3. **Заменить SVG-заглушки** в `public/images/` на реальные фото (hero-1..3, gallery-1..6)

---

## SEO + адрес + отзывы (2026-05-17)

### Реализовано

- `app/layout.tsx` — добавлен OpenGraph (`title`, `description`, `locale: ru_RU`, `type: website`); JSON-LD `AutoPartsStore` с адресом, телефонами, email, часами работы
- `components/sections/ContactsSection.tsx` — subtitle обновлён на реальный адрес: "Уфа, ул. Пархоменко, 171"
- `components/sections/ReviewsSection.tsx` — subtitle упрощён; добавлены кликабельные ссылки "Яндекс.Карты" и "2ГИС" под заголовком секции

### Закрытые пункты P1
- ✓ JSON-LD LocalBusiness + OpenGraph в `app/layout.tsx`
- ✓ z-index:9999 на `body::before` — был исправлен ещё в сессии 10 (→ 0)
- ✓ ReviewsSection — убрана фраза "500 клиентов за 30 лет", добавлены ссылки на площадки отзывов

### Осталось из P1
- Заменить SVG-заглушки в `public/images/` на реальные фото (hero-1..3, gallery-1..6)

---

## Каталог из БД + подготовка к 1С (2026-05-15)

### Реализовано

**Новые файлы:**
- `lib/categories.ts` — тип `CatalogProduct`, `STATIC_CATEGORIES` (7 категорий), `detectCategorySlug(name)`, `PAGE_SIZE=50`
- `lib/db-catalog.ts` — Prisma-функции: `getProducts`, `getProductByArticle`, `getFeaturedProducts`, `getOrCreateCategoryId`

**Изменены:**
- `prisma/schema.prisma` — добавлено поле `brandName String?` на модель Product
- `app/api/products/route.ts` — категория определяется по названию товара (`detectCategorySlug`), сохраняется `brandName`, кеш categoryId на запрос
- `app/catalog/page.tsx` — async server component, читает из Prisma, поиск `?q=`, пагинация `?page=`
- `app/catalog/CatalogView.tsx` — полный рерайт: поиск по артикулу/названию, категории-табы, пагинация 50/стр, пустое состояние
- `app/catalog/[slug]/page.tsx` — статические slugs из STATIC_CATEGORIES, данные из Prisma
- `app/catalog/[slug]/[article]/page.tsx` — товар из Prisma, убран `generateStaticParams`
- `app/catalog/[slug]/[article]/AddToCartButton.tsx` — тип `CatalogProduct`
- `components/ui/ProductCard.tsx` — принимает `CatalogProduct`, опциональный `href`, ссылка на страницу товара
- `lib/cart-store.ts` — тип `CatalogProduct` вместо `MockProduct`
- `components/sections/CategoriesSection.tsx` — статические категории из `STATIC_CATEGORIES`
- `components/sections/ProductsSection.tsx` — принимает `products: CatalogProduct[]`, скрывается если пусто
- `components/sections/PartFinderSection.tsx` — убраны импорты mock-data, standalone
- `app/page.tsx` — async, `getFeaturedProducts(4)` из Prisma
- `lib/mock-data.ts` — удалены каталожные типы (MockProduct, mockProducts, mockCategories, FeaturedProduct, featuredProducts, mockModels); остались MockService/mockServices, MockReview/mockReviews

**Поведение при пустой БД:** каталог показывает заглушку "Товары появятся после первой синхронизации с 1С"; главная страница скрывает ProductsSection.

### Следующие шаги
1. **Supabase** — создать проект, получить `DATABASE_URL`
2. **Netlify env** — добавить `DATABASE_URL`, `SYNC_LOGIN`, `SYNC_PASSWORD`
3. **Миграция** — `npx prisma migrate deploy`
4. **Деплой** — задеплоить на Netlify, проверить `/api/products` и `/api/orders`
5. **Первая синхронизация** — 1С делает POST с реальным прайсом, проверяем каталог
6. **Фото товаров** — решить как быть без изображений из 1С (Supabase Storage или заглушка)

---

## Интеграция с 1С (2026-05-14)

### Реализовано

**`prisma/schema.prisma`**
- `Order.userId` — опциональный (гостевые заказы без авторизации)
- `Order` — добавлены поля `name`, `phone`, `syncedAt`
- `OrderItem.productId` — опциональный (товар может быть не в БД)
- `OrderItem` — добавлены поля `article`, `itemName`

**`app/api/order/route.ts`** — обновлён
- Заказы теперь сохраняются в БД перед отправкой в Telegram
- Telegram-отправка не блокирует сохранение (если токен не задан — просто пропускается)

**`app/api/orders/route.ts`** — новый
- `GET /api/orders` — для 1С: отдаёт заказы со статусом `PENDING` в CSV (разделитель `;`)
- Формат: `Номер_Заказа;Дата;Имя;Телефон;Артикул;Наименование;Количество;Цена;Комментарий`
- После выдачи автоматически помечает заказы как `PROCESSING` + записывает `syncedAt`
- Авторизация: Basic Auth (`SYNC_LOGIN` / `SYNC_PASSWORD` в env)

**`app/api/products/route.ts`** — новый
- `POST /api/products` — для 1С: принимает CSV с разделителем `;`
- Формат входящих данных: `Код;Код_Каталога;Код_Поставщика;Наименование;Артикул;Бренд;Цена1;Остаток`
- Upsert по `externalId` (поле `Код` из 1С)
- Категории создаются автоматически по `Код_Каталога`
- Авторизация: Basic Auth

### Env-переменные (добавить)
```
SYNC_LOGIN=логин
SYNC_PASSWORD=пароль
```

### Что ещё нужно
- Получить логин/пароль от специалиста 1С для тестирования
- Уточнить формат ответа на заказы если 1С ожидает что-то иное

---

## P2 + P3 аудита UX/UI (2026-04-23, сессия 11)

### P2 — Новые компоненты

- `components/sections/BrandsSection.tsx` — 4 карточки брендов (ГАЗ — "Официальный дилер", УАЗ — "Субдилер"), staggered Framer Motion, hover border-red. Вставлена в `app/page.tsx` после AdvantagesSection
- `components/sections/PartFinderSection.tsx` — client-компонент: select Марка → Модель → Категория. При выборе марки обновляет список моделей. Кнопка "Найти запчасть" → `/catalog?brand=...&model=...&category=...`. Вставлена после CategoriesSection
- `components/ui/WhatsAppButton.tsx` — fixed bottom-right, зелёный (#25D366), анимация появления delay 1.5s, tooltip при hover. Добавлена в `app/layout.tsx`
- `lib/mock-data.ts` — добавлен `mockModels: Record<brand, string[]>` (5 моделей ГАЗ, 5 ВАЗ, 4 УАЗ, 4 КАМАЗ)

### P3 — Улучшения

- `components/sections/AdvantagesSection.tsx` — emoji (`✓`, `⚡`, `🛡`, `30+`) заменены на inline SVG-иконки (checkmark, lightning, shield, clock)
- `components/layout/Footer.tsx` — динамический год уже был реализован в сессии 10 (`new Date().getFullYear()`)
- `app/sitemap.ts` — создан, 4 URL: `/`, `/service`, `/catalog`, `/about`

`npx tsc --noEmit` — ✓

---

## Данные + анимации (2026-04-22, сессия 10)

**Исправление данных:**
- `docs/BRIEF.md` — разделены марки: магазин (ГАЗ/ВАЗ/УАЗ/КАМАЗ) vs сервис (ГАЗ/ВАЗ/УАЗ + иномарки); добавлено примечание об ограничениях (нет шиномонтажа и кузовного ремонта)
- `CLAUDE.md` — добавлена секция "ОГРАНИЧЕНИЯ СЕРВИСА"
- `lib/mock-data.ts` — тип group расширен (`'Электрика'`); добавлены 3 услуги: Ремонт КПП, Ремонт генератора, Ремонт стартера (итого 17 услуг)

**Баг-фикс:**
- `app/globals.css` — `body::before` z-index исправлен с 9999 → 0 (ранее перекрывал focus rings)

**Анимации (Framer Motion, без новых зависимостей):**
- `components/sections/HeroSection.tsx` — parallax на фоновые градиенты (useScroll + useTransform); animated counter для "50 000+" от 0 до значения при появлении в viewport
- `components/sections/AdvantagesSection.tsx` — hover lift на карточках (y: -4px, red shadow)
- `components/layout/Footer.tsx` — TrustCounter анимирует числа "30+" и "50 000+" при скролле до футера; динамический год через `new Date().getFullYear()`; добавлен "Ремонт КПП" в список сервиса

`npm run build` — ✓

---

## UX/UI Аудит (2026-04-22, сессия 9)

**Проведён полный аудит — код изменений не вносился, только план.**

### P1 — Критические проблемы (реализовать первыми)
1. `HeroSection.tsx` — CTA "Записаться на СТО" открывает href=/service вместо BookingModal
2. `ContactsSection.tsx` — нет реального адреса улицы; нет embed Яндекс.Карт
3. `app/layout.tsx` — нет JSON-LD LocalBusiness, нет OpenGraph тегов
4. `app/globals.css` — `body::before` имеет z-index:9999, перекрывает focus rings (accessibility)
5. `ReviewsSection.tsx` — subtitle "500 клиентов за 30 лет" звучит как антидоверие

### P2 — Новые компоненты к созданию
- `BrandsSection.tsx` — логотипы брендов поставщиков
- `PartFinderSection.tsx` — подбор запчастей по марке/модели/категории
- WhatsApp floating кнопка

### P3 — Улучшения
- Emoji иконки в AdvantagesSection → SVG
- Footer: динамический год
- mock-data: новые группы услуг (Шины, Кузов/кондиционер, Электрика)
- `app/sitemap.ts` — создать для SEO

### Тема
Рекомендована гибридная: Header/Hero/Footer тёмные, остальные секции светлые (#F5F7FA).
Палитра задокументирована в CLAUDE.md.

### Шрифты
Текущие Russo One + IBM Plex Sans — оставить. IBM Plex Mono — только для артикулов деталей.

---

## Последние изменения (2026-04-22, сессия 8)

**Выполнено — точечный рефакторинг:**
- `components/layout/Header.tsx` — nav-ссылки `/catalog` и `/delivery` скрыты (закомментированы до реализации)
- `components/sections/HeroSection.tsx` — badge синхронизирован с BRIEF.md; CTA "Найти запчасть" → "Наши услуги" (href `/service`)
- `components/layout/Footer.tsx` — статус компании синхронизирован с BRIEF.md; добавлен второй номер сервиса `+7 903 311-16-45`
- `components/sections/ContactsSection.tsx` — tel: ссылки нормализованы (явный формат `tel:+7...`); заменён map-плейсхолдер на кнопку "Открыть на Яндекс.Картах"
- `app/about/page.tsx` — стат "3 бренда" → "4 бренда (ВАЗ, ГАЗ, УАЗ, КАМАЗ)"; сертификаты: document-иконки → SVG-заглушки через `<Image>`
- `lib/mock-data.ts` — `MockService` расширен полем `group`; 4 сервиса → 14 с группировкой по 4 категориям
- `app/service/page.tsx` — рендер услуг по группам с заголовком группы
- `components/ui/ProductCard.tsx` — `<img>` → `<Image>` из next/image, убран eslint-disable
- `components/ui/Button.tsx` — добавлены `focus-visible` стили (ring) для всех вариантов
- `docs/DESIGN.md` — обновлены ссылки на шрифты: Google Fonts → локальные файлы `public/fonts/`
- `app/layout.tsx` — metadata description синхронизирован: добавлен "субдилер УАЗ и ЗМЗ"
- `next.config.mjs` — добавлен `images: { unoptimized: true }`
- `netlify.toml` — создан (build command + @netlify/plugin-nextjs)
- `public/certificates/cert-{1-4}.svg` — созданы SVG-заглушки сертификатов

`npm run build` — ✓

---

## Последние изменения (2026-04-20, сессия 7)

**Выполнено:**
- `app/layout.tsx` — `next/font/google` заменён на `next/font/local`; шрифты берутся из `public/fonts/` без сетевых запросов при сборке
- `public/fonts/` — добавлены 16 woff2-файлов: Russo One, IBM Plex Sans (400/500/600/700), IBM Plex Mono (400/500); latin + cyrillic сабсеты
- `.env.example` — добавлено предупреждение: `HTTP_PROXY`/`HTTPS_PROXY` только для локальной разработки, не ставить на Netlify
- `npm run build` — прошёл успешно ✓

**Причина:** `next/font/google` пытался скачать шрифты через `HTTP_PROXY=127.0.0.1:12334` во время `next build`, что падало на Netlify (нет локального прокси)

---

## Последние изменения (2026-04-20, сессия 6)

**Выполнено:**
- `.gitignore` — добавлены `.env`, `.env.local`, `.env*.local`, `.next`, `.DS_Store`; ранее секреты не были защищены
- `.env.example` — добавлены `TELEGRAM_CHAT_ID`, `TELEGRAM_CHAT_ID_2`, `HTTPS_PROXY`, `HTTP_PROXY`; все значения пустые
- Проверено: токены нигде не захардкожены в коде — только `process.env`

**⚠️ Внимание:** если `.env.local` был закоммичен до этой сессии — токен бота нужно отозвать через BotFather

---

## Последние изменения (2026-04-20, сессия 5)

**Выполнено:**
- `components/ui/BookingModal.tsx` — порядок полей: Имя → Телефон → Авто; маска телефона `+7 (XXX) XXX-XX-XX`; валидация имени (regex 2–50 символов) и телефона (11 цифр) с ошибками под полями (blur + submit); honeypot поле; rate-limit 30 сек между отправками
- `app/api/booking/route.ts` — строгая zod-валидация: regex для имени и телефона, серверная проверка honeypot

---

## Последние изменения (2026-04-20, сессия 4)

**Выполнено:**
- `app/api/booking/route.ts` — POST endpoint с zod-валидацией, отправляет заявку в два Telegram чата (`TELEGRAM_CHAT_ID`, `TELEGRAM_CHAT_ID_2`), поддержка прокси через `HTTPS_PROXY`
- `components/ui/BookingModal.tsx` — подключена отправка на `/api/booking`, состояния loading/success/error, сброс формы при закрытии
- `.env.local` — добавлены переменные `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`

---

## Последние изменения (2026-04-20, сессия 3)

**Выполнено:**
- `app/layout.tsx` — `ContactsSection` вынесен в общий layout (отображается на всех страницах перед футером)
- `app/page.tsx` — убран дублирующий `ContactsSection`
- `components/ui/ServiceBookingCTA.tsx` — новый client-компонент: телефон + кнопка, открывающая `BookingModal`
- `app/service/page.tsx` — CTA-блок заменён на `<ServiceBookingCTA />`, кнопка теперь открывает модальное окно

**Изменённые файлы:**
- `app/layout.tsx`
- `app/page.tsx`
- `app/service/page.tsx`
- `components/ui/ServiceBookingCTA.tsx` (новый)

---

## Последние изменения (2026-04-20, сессия 2)

**Выполнено:**
- `app/service/page.tsx` — кнопка «Позвонить в сервис» → «Записаться на СТО»
- `components/layout/Header.tsx` — `sticky` → `fixed w-full`, hover навигации → синий `#2563EB`
- `app/layout.tsx` — favicon `/logo.png` добавлен в metadata, обёртка `pt-16` для fixed-хедера
- `app/about/page.tsx` — создана страница /about (Hero, Stats, О нас, Преимущества, Сертификаты-плейсхолдеры, CTA)
- Синий `#1A3A6B`/`#2563EB` применён как фирменный акцент (stats-секция, hover, бейджи, карточки сертификатов)

**Изменённые файлы:**
- `app/service/page.tsx`
- `app/layout.tsx`
- `app/about/page.tsx` (новый)
- `components/layout/Header.tsx`

**Примечание по сертификатам:**
Карточки сертификатов готовы к замене на реальные фото — добавить изображения в `public/certificates/cert-1.jpg` ... `cert-4.jpg`.

---

## Последние изменения (2026-04-20)

**Выполнено:**
- Логотип в хедере заменён на текстовый «ТРАК»
- Кнопки «Позвонить в сервис» / «Записаться на сервис» → «Записаться на СТО»
- В модалке добавлено поле «Марка и модель авто», заголовок обновлён
- Категории товаров переименованы («Для двигателя», «Для подвески» и т.д.)
- Шиномонтаж заменён на «3D развал-схождение»
- В HeroSection добавлен синий градиент слева + акцентная полоска под badge

**Изменённые файлы:**
- `components/layout/Header.tsx`
- `components/ui/BookingModal.tsx`
- `components/sections/HeroSection.tsx`
- `components/sections/ServiceSection.tsx`
- `lib/mock-data.ts`

---

## Что сделано

### 1. Инициализация проекта
- Стек: Next.js 14, TypeScript, Tailwind CSS v3, Framer Motion, Prisma 5, Zustand, react-hook-form, zod, NextAuth.js
- Ручная инициализация (create-next-app конфликтовал с CLAUDE.md)
- Фиксы версий: Prisma 5, Tailwind 3, next.config.mjs, types/css.d.ts, tsconfig target ES2017

### 2. Дизайн-система
- `tailwind.config.ts` — бренд-цвета, шрифты (Russo One / IBM Plex Sans / IBM Plex Mono)
- `app/layout.tsx` — Google Fonts через next/font
- `app/globals.css` — CSS-переменные, noise overlay

### 3. База данных (`prisma/schema.prisma`)
- Модели: Brand, CarModel, Category (materialized path), AttributeDefinition, Product, ProductAttribute (valueString/valueNumber), ProductBrand, ProductCompatibility, ProductImage, Order, OrderItem, Booking, Review, Article, User/Account/Session
- Составные индексы для фильтрации каталога 50k+
- Поля интеграции на Product: `externalId`, `source`, `syncedAt`

### 4. Mock-данные (`lib/mock-data.ts`)
- `MockProduct` — 12 товаров (VAZ/GAZ/UAZ/KAMAZ)
- `MockReview` — 3 отзыва
- `MockCategory` — 6 категорий со slug, icon, count
- `MockService` — 4 услуги с ценой и длительностью
- `FeaturedProduct` — универсальный тип (id, name, article, price, stock, imageUrl?)

### 5. Главная страница — UI MVP (`npm run build` ✓)

**Компоненты:**
- `components/layout/Container.tsx` — max-w-[1280px] обёртка
- `components/layout/Header.tsx` — sticky, телефон, CTA кнопка, мобильное меню (AnimatePresence)
- `components/layout/Footer.tsx` — строка доверия (30+/50k+/дилер), 4 колонки, реальные контакты
- `components/ui/Button.tsx` — primary/secondary/ghost, поддержка href
- `components/ui/SectionHeading.tsx` — title + subtitle, align
- `components/ui/ProductCard.tsx` — бейдж "В наличии"/"Под заказ", hover red glow
- `components/ui/ReviewCard.tsx` — звёзды, дата

**Секции (`components/sections/`):**
- `HeroSection` — fullscreen, noise overlay, Framer Motion
- `AdvantagesSection` — 4 карточки преимуществ
- `CategoriesSection` — grid 3×2, Link → /catalog/[slug], hover red border + полоска
- `ProductsSection` — featuredProducts, бейджи наличия
- `ServiceSection` — две колонки, hover "Записаться" на карточках
- `ReviewsSection` — mockReviews
- `ContactsSection` — телефоны, часы, плейсхолдер карты

---

## Следующие шаги

1. **Страница каталога** — `/catalog` и `/catalog/[slug]` с фильтрацией на mock-данных
2. **Страница товара** — `/catalog/[slug]/[article]`
3. **Страница сервиса** — `/service` с формой записи
4. **Catalog API** — спроектировано, не реализовано (`app/api/products/route.ts`, `lib/catalog.ts`)
5. **Корзина** — Zustand store, UI

---

## Известные фиксы

| Проблема | Решение |
|---|---|
| Prisma 7 сломал datasource url | Downgrade до Prisma 5 |
| Tailwind v4 несовместим с tailwind.config.ts | Downgrade до Tailwind v3 |
| next.config.ts не поддерживается Next.js 14 | Переименовать в next.config.mjs |
| TypeScript ошибка при импорте CSS | Создать types/css.d.ts |

---

## Для следующей сессии

Минимальный набор файлов для старта:

```
docs/PROGRESS.md       — этот файл
docs/DESIGN.md         — цвета, типографика, анимации
docs/BRIEF.md          — контакты, марки, услуги
lib/mock-data.ts       — все типы и данные
app/page.tsx           — точка входа главной
prisma/schema.prisma   — структура БД (для API и каталога)
```
